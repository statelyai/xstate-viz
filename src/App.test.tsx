import * as React from 'react';
import { render } from '@testing-library/react';
// import App from './App';

describe('<App>', () => {
  it('testing works', () => {
    expect(1 + 1).toEqual(2);
  });
});
